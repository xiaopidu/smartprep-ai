/**
 * 主题配置：暖色渐变配色方案
 * 
 * 设计说明：
 * - 主色调从橙色过渡到粉色，营造温暖友好的学习氛围
 * - 保持足够的对比度确保可读性
 * - 动画效果轻柔自然
 */

// ============================================================================
// 色彩常量
// ============================================================================

/** 主渐变色 - 橙色到粉色 */
export const COLORS = {
  // 渐变起始色（橙色系）
  warmOrange: {
    50: '#fff7ed',
    100: '#ffedd5',
    200: '#fed7aa',
    300: '#fdba74',
    400: '#fb923c',
    500: '#f97316',
    600: '#ea580c',
  },
  
  // 渐变终止色（粉色系）
  warmPink: {
    50: '#fdf2f8',
    100: '#fce7f3',
    200: '#fbcfe8',
    300: '#f9a8d4',
    400: '#f472b6',
    500: '#ec4899',
    600: '#db2777',
  },
  
  // 功能色
  favorite: '#f472b6',   // 收藏色（粉色）
  mistake: '#ef4444',    // 错题色（红色）
  note: '#a855f7',       // 笔记色（紫色）
  correct: '#22c55e',    // 正确色（绿色）
  incorrect: '#ef4444',  // 错误色（红色）
} as const;

// ============================================================================
// 渐变样式
// ============================================================================

/** CSS 渐变字符串 */
export const GRADIENTS = {
  /** 主背景渐变 */
  primary: 'linear-gradient(135deg, #fff7ed 0%, #fdf2f8 50%, #fce7f3 100%)',
  
  /** 按钮渐变 */
  button: 'linear-gradient(135deg, #fb923c 0%, #f472b6 100%)',
  
  /** 卡片悬停渐变 */
  cardHover: 'linear-gradient(135deg, #fed7aa 0%, #fbcfe8 100%)',
  
  /** 侧边栏激活项渐变 */
  navActive: 'linear-gradient(135deg, #ffedd5 0%, #fce7f3 100%)',
  
  /** 收藏按钮渐变 */
  favorite: 'linear-gradient(135deg, #fce7f3 0%, #fbcfe8 100%)',
} as const;

// ============================================================================
// 动画配置
// ============================================================================

/** 动画时长（毫秒） */
export const ANIMATION = {
  fast: 150,
  normal: 300,
  slow: 500,
} as const;

/** 缓动函数 */
export const EASING = {
  easeOut: 'cubic-bezier(0.33, 1, 0.68, 1)',
  easeInOut: 'cubic-bezier(0.65, 0, 0.35, 1)',
  bounce: 'cubic-bezier(0.34, 1.56, 0.64, 1)',
} as const;

// ============================================================================
// 尺寸配置
// ============================================================================

/** 间距 */
export const SPACING = {
  xs: '4px',
  sm: '8px',
  md: '12px',
  lg: '16px',
  xl: '24px',
  xxl: '32px',
} as const;

/** 圆角 */
export const RADIUS = {
  sm: '6px',
  md: '8px',
  lg: '12px',
  xl: '16px',
  full: '9999px',
} as const;

// ============================================================================
// 主题对象（供组件使用）
// ============================================================================

export const theme = {
  colors: COLORS,
  gradients: GRADIENTS,
  animation: ANIMATION,
  easing: EASING,
  spacing: SPACING,
  radius: RADIUS,
} as const;

export default theme;
